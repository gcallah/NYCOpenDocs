Defines a tuple of values as PermissionPair, matching a permission value to a permission label. 
<br>Request information permissions and response permissions are assigned a value. Then PermissionPairs are formed by the values and the associated title names of the permissions.
<br>Example: PermissionPair(CLOSE, "Close Request"), where CLOSE represents the permission value for closing a request.