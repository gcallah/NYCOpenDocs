Blueprints are used in Flask to maintain a large and scalable application.
<br>This file helps in creating an admin interface accessible to only a
subset of users (users with an admin role).
<br>
All the view functions (the ones with a route() decorator on top) have to be
imported in the __init__.py file.
